name: 02 - Workflow Events
on: 
    workflow_dispatch: 
    pull_request: 
        branches: [main]
    schedule:
        - cron: '55 10 30 1 4'
jobs:
    echo-evt:
        runs-on: ubuntu-latest
        steps:
            - name: echo trigger
              run: |
                   date
                   echo "Event name: ${{github.event_name}}"
